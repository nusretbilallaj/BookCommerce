@using BookCommerce.Model
@model List<BookCommerce.Model.Mbulesa>

@if (TempData["suksesi"] != null)
{
    <script src="~/lib/jquery/dist/jquery.js"></script>
    <script src="//cdnjs.cloudflare.com/ajax/libs/toastr.js/latest/toastr.min.js"></script>
    <script type="text/javascript">
        toastr.success('@TempData["suksesi"]');
    </script>
}

<div class="container">
    <div class="row">
        <div class="col-6">
            <h1 class="text-primary">Mbulesat</h1>
        </div>
        <div class="col-6 text-end">
            <a asp-controller="Mbulesa" asp-action="Krijo" class="btn btn-primary">Shto</a>
        </div>
    </div>
    <table class="table table-bordered table-striped">
        <thead>
        <tr>
            <th>Id</th>
            <th>Emri</th>
            <th>Veprimet</th>
        </tr>
        </thead>
        <tbody>
        
            @foreach (Mbulesa mbulesa in Model)
            {
                <tr>
                    <td>
                        @mbulesa.Id
                    </td>
                    <td>
                        @mbulesa.Emri
                    </td>
                    <td>
                        <div class="btn-group">
                            <a asp-controller="Mbulesa" asp-action="Ndrysho" asp-route-id="@mbulesa.Id" class="btn btn-primary mx-2">Ndrysho</a>
                            <a asp-controller="Mbulesa" asp-action="Fshi" asp-route-id="@mbulesa.Id" class="btn btn-danger mx-2">Fshi</a>
                        </div>
                    </td>
                </tr>
            }
           
        </tbody>
    </table>
</div>